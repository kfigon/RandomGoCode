package main

// let's assume minutes for now
const min = 0
const max = 60

// <start;stop>
type rng struct {
	start int
	stop int
	step int
}

func eval(toks []token) ([]int, error) {
	return nil,nil
}